{
  "schema_version": "synthetic-tree-backend-comparison-v1",
  "created_before_benchmark": true,
  "purpose": "local_training_runtime_selection_not_financial_validation",
  "seed": 20260904,
  "numpy_version": "2.2.6",
  "lightgbm_version": "4.7.0",
  "training_rows": [4096, 65536, 262144],
  "features": 32,
  "validation_rows": 4096,
  "boosting_rounds": 50,
  "repetitions": 3,
  "threads": 8,
  "max_bin": 63,
  "num_leaves": 31,
  "gpu_platform_id": 0,
  "gpu_device_id": 0,
  "gpu_use_dp": true,
  "dataset": "PCG64 standard_normal float32; label = feature0 + 0.5*feature1 - 0.2*feature2 + independent standard_normal*0.5 > 0",
  "ordering": "even repetition cpu then gpu; odd repetition gpu then cpu",
  "timing": "Dataset construction, training and prediction on held-out synthetic rows; synthetic data generation excluded equally; warm-up reported separately",
  "warmup_rows": 512,
  "warmup_rounds": 1,
  "max_absolute_prediction_difference": 0.001,
  "max_absolute_logloss_difference": 0.00001,
  "minimum_median_speedup_to_prefer_gpu": 1.1,
  "required": ["all predictions finite", "exact train/save/reload prediction equality within each fit", "all paired comparisons meet the fixed tolerances", "retain every repetition including failures"],
  "scope_limits": ["no market data", "no profitability claim", "no transfer of qualification to frozen historical models", "no global default changes from one synthetic benchmark", "device peak memory not measured by this test"]
}
