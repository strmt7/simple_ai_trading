{
  "schema_version": "synthetic-tree-backend-comparison-v1",
  "created_before_benchmark": true,
  "purpose": "large_workload_followup_after_small_workloads_showed_GPU_overhead_not_independent_financial_validation",
  "seed": 20260905,
  "numpy_version": "2.2.6",
  "lightgbm_version": "4.7.0",
  "training_rows": [1048576],
  "features": 128,
  "validation_rows": 4096,
  "boosting_rounds": 200,
  "repetitions": 3,
  "threads": 8,
  "max_bin": 63,
  "num_leaves": 31,
  "gpu_platform_id": 0,
  "gpu_device_id": 0,
  "gpu_use_dp": true,
  "dataset": "PCG64 standard_normal float32; label = feature0 + 0.5*feature1 - 0.2*feature2 + independent standard_normal*0.5 > 0",
  "ordering": "even repetition cpu then gpu; odd repetition gpu then cpu",
  "timing": "Dataset construction, training and heldout prediction; generation excluded equally; warmup separate",
  "warmup_rows": 512,
  "warmup_rounds": 1,
  "max_absolute_prediction_difference": 0.001,
  "max_absolute_logloss_difference": 0.00001,
  "minimum_median_speedup_to_prefer_gpu": 1.1,
  "required": ["all predictions finite", "exact train/save/reload equality", "all paired comparisons within unchanged tolerances", "retain all repetitions and failures"],
  "scope_limits": ["synthetic only", "not a profitability test", "not independent of initial performance observations", "no precision relaxation", "no frozen historical model changes", "device peak memory unmeasured", "no universal GPU threshold inferred"]
}
