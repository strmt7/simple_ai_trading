# Testnet authentication and bounded literature discovery

## Authenticated testnet result

At 2026-09-05 02:14 UTC, the user-supplied credentials authenticated successfully
against the existing app configuration's **Spot testnet**. One public clock GET
and one signed BTCUSDT commission GET returned HTTP 200. All returned standard,
special and tax maker/taker/buyer/seller rates were zero. This is testnet-only
configuration, not mainnet fee eligibility or profitable execution evidence.

No order, cancellation, transfer, deposit, mainnet access or other account
mutation occurred. Credentials were supplied through masked process input,
not script literals, shell command arguments, persistent environment variables
or repository files. Terminal echo was first checked using a non-secret dummy.
The process exited; no durable credential installation or future autonomous
access is claimed. Python memory clearing is not guaranteed secure erasure.
Conversation/tool-input records are distinct from the repository and this probe's
sanitized terminal output and application journal; no claim is made to erase chat.

The [plan](testnet-readonly/plan.json), [sanitized result](testnet-readonly/result.json)
and request journal record the exact read-only scope. The
[official source extraction](testnet-readonly/official-source-extraction.json)
records the endpoint classification and base host from Binance's current
[Spot testnet REST documentation](https://developers.binance.com/en/docs/products/spot/testnet/rest-api).
No raw private response, key fingerprint, signature, request headers, signed URL,
balance, account identifier or free-form remote error is persisted. The consumed
probe is not a trading runner and must not be rerun.

## Literature discovery, not economic evidence

Two frozen searches returned four primary-paper leads, not a complete catalog.
The August 31 semi-static-market paper predates the checkpoint cutoff. The
September AMM-design and LLM-agent experiment leads do not themselves establish
a deployed Binance/Polymarket mechanism. None of their abstract performance
figures is admitted as trading evidence.

The distinct September 1 XiNet paper, arXiv 2609.01941, was absent from retained
identifier/title/author aliases and appeared relevant to rank 10's option-implied
prediction-market wedge. Its abstract is only a methodology lead. The single
frozen HTML-v1 GET returned **HTTP 404**, with 7,901 response bytes retained.
There was no full-paper review, verified neural-model uplift, market-data request,
new model fit, or changed payoff/acceptance gate. Preserve the
[contract and journal](xinet-source/contract.json); do not retry PDF/version/URL
aliases under this consumed source contract. The method remains unverified,
not disproved. Resume only on independently available materially new complete
primary methodology or reproducible evidence, not the indexed abstract alone.

Efficiency correction: a paper identifier does not establish that arXiv's HTML
representation exists. Future source selection should use a source-exposed
available representation before committing an exact-format capture. This 404
must not be used as evidence against the paper's conclusions.

Registry counts remain 37 accepted scopes, 65 hypotheses, 189 terminal market
observations and zero current fully qualified edges. The rank-10 attachment is
source-only; previous results, protected captures and literal market retry gates
are unchanged. The successful testnet check does not unlock mainnet fee evidence.
