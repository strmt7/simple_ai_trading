# September 4 comprehensive review — in progress

The [whole-product reassessment](review/2026-09-04/product-reassessment.md)
extends this pass to ideas, plans, native/console interfaces and architecture.
It records implemented display corrections separately from proposed workflow
changes. [Dependency remediation](review/2026-09-04/dependency-advisories.md)
records the three imported advisories and the narrowly verified optional pin.

## Outcome and scope

No stable, currently account-qualified, after-all-cost market edge is proved.
The 37 accepted scopes are a mechanism ledger, not 37 profitable strategies.
The current registry contains 65 hypotheses and 189 terminal observations.
The unfinished September USD1 source checkpoint has been reconciled; the new
campaign must not be confused with the expired August campaign.

The user requested a comprehensive review, with stable direction-neutral edge
discovery ahead of repetitive tests. There is no zero-mistake guarantee and no
claim that all code has now received a semantic review. No account, credential,
order, fund, protected capture, or new venue request was used for the initial
inventory review. Later public source requests are recorded in dated sidecars.

Base HEAD: `976d725f8e55525187eb7cce8b9bca34604439af`.
The file-level snapshot is [inventory.tsv](review/2026-09-04/inventory.tsv),
with scope and content binding in [summary.json](review/2026-09-04/summary.json).
It inventories 4,648 tracked files. Exactly 1,856 Python files under the selected
code directories were AST-parsed without importing them: 964,526 lines and
28,265 function definitions, with no parse errors. The other 2,792 files are
metadata-only, not reviewed content. Protected/raw data was not opened.
Untracked additions, including the new inventory utility, are excluded from
that snapshot and reviewed separately. No inventory row asserts correctness.

## Findings and decisions

R24: the [owned execution campaign](review/2026-09-05/spot-testnet-execution-review.md)
completed nine virtual Spot orders, three cancellations and six reconciled
trades across BTC/ETH/SOL, ending with zero owned residual and no open orders.
Fixed a reporting-only cancellation client-ID assumption in forward code;
preserved the exact executed source and original failed reporting flag.
The separately bound offline replay verifies numeric order identity, DELETE
receipts and exact cash. Thirty-three focused tests passed. No mainnet economic
claim, market observation, cold-restart coverage or whole-repo completion follows.

R23: the [testnet and literature checkpoint](review/2026-09-05/testnet-and-literature-status.md)
records successful user-authorized Spot-testnet authentication without orders
or credential persistence, separately from a new option-model source lead whose
HTML request returned 404. No mainnet fee evidence, full-paper methodology,
neural-model uplift or profitable edge is established. The one-use probe was
read and its safe-output/retry boundaries are covered by focused offline tests.

R22: the [carry turnover/liquidity audit](review/2026-09-05/carry-turnover-liquidity-review.md)
reconstructs all 17 retained symbols with no new request. Nine funding subtotals
are positive after one modeled round trip, versus eight after three; none
clears the unchanged original capital stress. Funding-only cash paths expose
interim financing needs, not sufficient margin buffers. Old results and gates
remain unchanged. Missing account credentials block only that branch, not all
useful offline/public research. The new runner and focused tests were read;
this is explanatory accounting, not a new validation or profitable-edge claim.

R21: the [within-event CPI partition screen](review/2026-09-04/cpi-partition-review.md)
uses the same retained sources without an annual/monthly mapping. All 42 rows
across both fully price-complete events fail before fees. The separate frozen
offline question reuses the existing frontier implementation; no price capture,
historical rewrite, cross-event floor or fresh validation is claimed. The new
wrapper and three tests were fully read. This exact screen is terminal.

R20: the [CPI mapping review](review/2026-09-04/inflation-mapping.md) binds
21 exact annual/monthly contract definitions and two BLS method extractions.
It rejects same-scalar/single-factor hedge inference across different seasonal
adjustment bases without a complete aggregation contract. A conditional
component-ratio bound remains a research lead, not an admitted venue model.
No market prices or historical results were screened or changed. The new
definition capture, review and tests were fully read; no production adapter
or training model was enabled.

R19: [Paradex fee-route qualification](review/2026-09-04/paradex-fee-routes.md)
separates the prior study's stress allowance from documented order-classified
fees. Two retained official Markdown pages establish source terms, not actual
commissions, workflow eligibility or net execution improvement. Rank 59 gains
source evidence without a new terminal observation or changed retry trigger.
The review/parser and bounded documentation capture helper were fully read;
no historical study was modified or new trading adapter enabled.

R18: the [new Paradex index-boundary study](review/2026-09-04/paradex-index-review.md)
replaces an unnecessary dense-rate approximation with cumulative index changes
on a distinct prospectively fixed population. All 84 source responses passed;
78 fixed-base intervals and nine roles reconstructed, but no asset covers
even a single 20-bp execution allowance across the entire window. The public
gross spread is small, not absent; actual account fees/PnL and currency
conversion remain unqualified. Existing failed data and old results were not
reopened. This is a new scoped economic rejection, not a family-wide proof.

R16/R17: the [maker-execution semantics audit](review/2026-09-04/maker-execution-semantics.md)
reproduces equal-quote-notional residual inventory and partial-fill censoring
in the retained support infrastructure. Both-full does not prove flat; neither-
full does not prove no exposure. Historical support statistics and rejection
decisions are unchanged. The new offline cash/base diagnostic, source bindings
and forward-use instructions make those limits explicit; a quantity-aware
successor is still required before treating support labels as lifecycle PnL.
The Polymarket illustration gives a conditional stress break-even, not an
empirical fill estimate or a new accepted edge. Literature lookup ordering
also fell short of the existing deduplication rule and is recorded explicitly.

R15: the fresh prospectively selected NYC September 5 complete-package screen
found tiny gross discounts but no frozen fee/tick survivor. A separate
[retained fee-only audit](review/2026-09-04/nyc-sep5-frontier/review.md)
shows configured fees alone eliminate all 17 evaluated rows, before any
adverse tick. The gross leader's 0.060 pUSD becomes negative 0.09394 pUSD.
Missing NO sides remain incomplete. This is a real new public observation,
not a profitability claim or a retry of a consumed date. The bounded capture
bridge also addresses R8 for this new runner without changing old captures.

R14: the broad spot/perpetual funding prefilter uses unit-weight rate sums and
a labeled 10%-per-leg annual capital stress. The
[full-population cash-flow audit](review/2026-09-04/spot-funding-cashflow-review.md)
reconstructs every original role and weights fixed-base funding by its actual
settlement mark. All three majors still have at least one negative role after
the unchanged 32-bp execution sensitivity even at zero capital hurdle.
Three other pairs have positive subtotals across roles but only roughly
0.21%-0.49% weakest-role annual capital-cost headroom per reference leg before
omitted terms. This clarifies cash versus stress without promoting or rerunning
the consumed test or authorizing further sampling.

R13: the distinct official clearing-procedures PDF has a visually confirmed
COIN-M PnL sign conflict between sections 80 and 86.2. Its maintenance,
coin-funding debits and delivery-index average also constrain the inverse
collateral hypothesis. The [source review](review/2026-09-04/inverse-clearing-source-review.md)
retains the complete body and request journal; no current payoff qualification
or new market-data authorization follows. Historical contract metadata was
reused without opening funding outcomes or modifying old results.

R12: inverse collateral needs explicit coin cash-flow and spot/mark accounting.
The [conditional inverse review](review/2026-09-04/inverse-collateral-economics.md)
shows how exact matching cancels common-price exposure and coin debits/credits
restore it. Three FAQ requests redirected and did not verify current mechanics;
no market capture or previous result was rerun. A research-only evaluator and
[staged economic R&D plan](review/2026-09-04/economic-rd-rounds.md) now specify
the source, cash-flow, data and causal-model gaps before further collection.

R11: the retained Backpack/Binance funding prefilter sums rates without each
settlement's changing mark/quote notional. The new
[funding-notional review](review/2026-09-04/funding-notional-review.md)
reconstructs all nine gross role sums and tests illustrative weight bounds
offline. The test roles remain negative even at the optimistic ±20% bound
under the unchanged frozen hurdle. This neither repairs the old test nor
qualifies actual cash flows. Original implementations/results are unchanged.

The [GPU runtime review](review/2026-09-04/gpu-runtime.md) records working
OpenCL training and a reusable launcher. Other concurrent PC tasks were not
monitored during the initial benchmarks, so observed speed ratios remain
provisional. The user-requested noninterference rule stopped the concurrency
test; no unrelated process was modified. Larger synthetic workloads do not
establish better market accuracy or economic returns.

The next model review and its economic development direction are recorded in
[model-evidence-reassessment.md](review/2026-09-04/model-evidence-reassessment.md).
R10: the model-lab selection-risk boundary treated absent/indeterminate evidence
as a pass. It now requires an affirmative noncontradictory report for every
objective and rejects nonfinite selected/walk-forward scores. Twelve old-code
counterexamples fail; the corrected affected domain passes. Existing score
haircuts remain heuristics, not a statistical Deflated Sharpe Ratio.

| ID | Finding | Disposition |
| --- | --- | --- |
| R1 | Several research rules conflate an unavailable guaranteed cash floor with an economic rejection of the strategy. Maximum orphan loss exceeding maximum rewards also does not, by itself, establish negative expected market-making profit. | Confirmed methodological overreach. Apply the session-only distinctions below; keep historical contract outcomes unchanged. |
| R2 | A fixed 50-bp hurdle and one zero-basis CRWD snapshot cannot determine the profitability of integrated funding carry. Displayed or synthetic acquisition proxies also need a proved bound direction before being used to rule out all executable alternatives. | Confirmed inference limitation, not an arithmetic defect in the exact CRWD result. No failed result is promoted or rerun. |
| R3 | `PLANNING.md` called elapsed August captures and old worktree counts the active experiment order; long handoffs mix current and historical counts. | Corrected current routing and labeled the old plan historical. Dates alone do not prove terminalization or integration. |
| R4 | `tools/screen_polymarket_logical_parity.py` groups deadlines by stem, normalized description and fee schedule, then orders them by `end_date`; it does not bind observation starts. `logical_parity.py` assumes the implication supplied by its caller. | Confirmed missing proof at this legacy interface. Do not reuse it to certify settlement identity. Preserve consumed implementation bindings; a future replacement needs explicit source/start/end/tie/exception attestation and a versioned lineage before use. |
| R5 | The WDC/NVDA family test pinned mutable registry/audit hashes and a global terminal count, repeating an inefficiency already corrected elsewhere. | Fixed: verify canonical self-hashes and linked family evidence, not unrelated mutable global values. Immutable WDC evidence remains pinned. |
| R6 | `commission.py` clamps values with `max`, which initially looked capable of silently accepting malformed exchange rates. | Not a demonstrated production bug: `BinanceClient.get_commission_rates` checks symbol identity and validates finite bounded rates before returning them; `StrategyConfig` normalizes its configured fee. No speculative core edit. Alternate client implementations still require their own contract. |
| R7 | `PolymarketFeeModel.__call__` has no independent positive finite quantity or role validation and returns early for disabled/exempt fees. | Primitive-level validation gap; downstream structural/logical walkers validate positive finite quantities. Not yet established as reachable unsafe order behavior. Trace remaining callers before a shared-core change; preserve historical source bindings. |
| R8 | Generic public capture uses an unbounded response read before checking its declared byte ceiling; intent is journaled, but transport failures need careful completion/recovery treatment. | Confirmed resource-bound weakness, not evidence that a prior capture exceeded its ceiling. Future transport must enforce the ceiling during reading and preserve partial/error receipts without retry. Do not change a consumed implementation in place. |
| R9 | Portfolio risk aligned equally sized return tails when timestamps did not intersect, and otherwise matched only the final timestamp even when return intervals differed. Nonfinite calculated returns could be replaced with zero. | Fixed in `portfolio_risk.py`: use both close-to-close endpoints, remove the positional fallback, reject duplicate endpoints and nonfinite returns. Four alignment counterexamples failed before the change; all 23 affected portfolio/model-lab tests now pass, including the overflow case and synchronized legitimate data. |

R4 counterexample: an earlier-deadline condition can observe an event before a
later-deadline condition even begins observing. The early condition resolves
YES and the later condition NO. Buying NO(early) + YES(later) then pays zero.
Equal text and ordered end times do not eliminate this state. The module's
generic arithmetic is correct only after the implication has been established.
The current detailed repository rules already demand observation-start proofs;
the legacy runner does not encode that requirement.

## Session-only research corrections

These distinctions apply to this resumed review, not to rewriting past tests or
relaxing execution authority:

1. **Unavailable guarantee:** a source does not prove a positive worst-case
   return. This blocks a guarantee claim, not all expected-value research.
2. **Stress failure:** a candidate loses under specified adverse costs. Report
   the cost scenario and break-even threshold; do not call the hurdle a
   measured fee or conclude that every plausible scenario loses.
3. **Negative expected-value evidence:** a defined decision rule loses after
   measured costs in its scoped causal sample. Include uncertainty, capacity,
   selection history and failed variants. It is not a universal theorem.
4. **Payoff counterexample:** a supposedly guaranteed package pays less than its
   claimed floor in a valid state. This invalidates that guarantee even if the
   state is rare; repair semantics before requesting prices.

Offline exploration may reuse retained public evidence with selection history
explicitly labeled. It does not restore an untouched holdout. A consumed exact
contract remains consumed, and its literal retry trigger remains mandatory.
Any eventual validation needs independent prospective data and after-cost
acceptance rules fixed before access. No private data or protected partial
capture is opened for exploratory convenience.

A displayed midpoint is not necessarily an executable ask or an acquisition
lower bound. An expensive synthetic route does not prove every direct route is
expensive. A cheap route at one instant does not prove fillability or persistence.
Label missing-price and proxy-only rejection results as discovery limitations.

## Economic questions worth answering next

### 1. Binance: integrated carry, not just an entry spread

For quantity-matched **linear**, same-unit long spot / short future exposure,
define basis `B = F - S`, net funding received `H`, and all other net costs `C`.
Before liquidation/default/contract mismatch, the cash-flow identity is:

`profit = q * (B_entry - B_exit) + H - C`.

Thus funding receipts must exceed `C + q * (B_exit - B_entry)` to break even.
A zero entry basis says nothing by itself about that inequality. Conversely,
positive funding does not guarantee profit when exit basis or financing costs
move adversely. Funding notionals vary with mark prices: being delta-matched
does not make funding receipts or capital needs invariant to every market state.

For a dated cash-settled future, account for the actual index settlement and
spot exit difference; do not assume perfect convergence to the owned spot
execution price. Return on total locked spot plus margin/reserve capital is not
the same quantity as basis yield divided by spot purchase notional. Include
collateral interest, borrow, fees, rebalancing, missed settlements, conversion,
liquidation buffer and the feasible alternative use of capital exactly once.

Next eligible existing trigger: the source-bound CXMT twelve-settlement
post-change study no earlier than `2026-09-06T08:10:00Z`. Its frozen 6/3/3 roles
and receiving orientation requirements remain unchanged. This review does not
authorize early sampling or change its hurdle after outcomes. Plan subsequent
research from the integrated cash-flow identity, not headline rates.

### 2. Polymarket: valid payoff packages plus realistic completion probability

First prove compatible resolution sources, observation intervals, boundaries,
ties and exception states. Then separate a completed package's payout from the
cost of reaching that package. Multi-leg maker execution is not atomic.

For an illustrative two-outcome execution model, let `p` be completion
probability, `G` the profit on completion, `L` the loss on failed completion,
and `c` an additional unconditional expected cost. Then
`EV = p*G - (1-p)*L - c`, so for `G+L > 0`, positive EV needs
`p > (L+c)/(G+L)`. This is an identity, not an estimate of `p` or a market result.
Queue position, conditional adverse selection, partial size, cancellation lag,
hedge depth and timeouts determine those inputs. Public trades do not prove
our hypothetical order would have filled. Rewards are a separate incremental
cash flow and cannot be double-counted with spread or assumed at full pool share.

This suggests research on completion-adjusted package profit and abstention
conditions, not repeated catalog snapshots looking only for taker arbitrage.
Retained-data exploration comes first; no old event or protected capture is
reopened, and no live quoting or incentive manipulation is authorized.

The retained [opposite-lock robustness result](model-research/action-value/polymarket-wallet-opposite-lock-robustness-result-v1-2026-08-31.json)
is a useful concrete example. Its five-tick plus 0.05-pUSD-per-lock scenario
retains 154.9035632142 pUSD aggregate historical locked cash flow, but only
43/66 positive locks, below its frozen 80-percent requirement. That is a failed
robustness gate, not a negative aggregate-cash-flow result. About 97.5 percent
of the stressed aggregate comes from BTC; positive ETH and SOL subtotals do not
establish balanced cross-asset persistence. The source is public-wallet history,
not an owned executable strategy or a justification for acquiring the first leg.
Keep that failure. A distinct prospective investigation could evaluate a
cost-aware abstention rule on independently existing inventory, but must not
filter the observed losing transactions after the fact and relabel the same
sample out-of-sample. No new request or acceptance is authorized here.

### 3. Promotions: secondary overlays, not the principal research objective

The new USD1 campaign is source-proved but its first-period APR and WLFI
valuation are unpublished. Review once at/after September 11, 18:00 UTC, or
on independently evidenced material official change, per the exact contract.
The 150-million-WLFI pool is not an individual return. Do not manufacture
volume/open interest, assume eligibility, or project the expired campaign's APR.
MRNA's existing September 8 route and RLUSD's September 11 route retain their
exact registry conditions. Do not spend new requests merely increasing counts.

## Substantive coverage and remaining work

Fully read for this review: `commission.py`, `polymarket_fees.py`,
`quarterly_carry.py`, `structural_parity.py`, `logical_parity.py`,
`tools/adjudicate_binance_crwdb_bstock_perpetual_topbook_v1.py`, and
`tools/hash_json_without_field.py`. The new inventory utility and its tests are
separately reviewed. Reviewed relevant sections, not entire files, of
`api.py`, `types.py`, `financial_sanity.py`,
`execution_lifecycle.py` (the lifecycle-plan builder),
`tools/screen_polymarket_exact_two_leg_package.py`, and
`tools/screen_polymarket_logical_parity.py`. Current handoff headers, registry
source bindings, campaign artifacts, and relevant instructions were checked;
the entire historical document corpus has **not** been semantically reviewed.

Verification at this checkpoint: 73 focused tests passed across inventory,
WDC/NVDA evidence, durability audit, commission, quarterly carry, structural
parity and logical parity. Ruff passed on the changed Python files. Seven
canonical self-hashes and four exact USD1 raw/journal hashes were independently
checked. The inventory and USD1 raw paths have explicit Git byte/line-ending
attributes so publication cannot silently normalize their recorded bytes.

Continue in this order, recording exact file/function coverage and evidence:

1. Carry/funding analyzers and payoff/fee/execution consumers; distinguish
   actual production defects from validated-boundary or caller-precondition cases.
2. Order ownership, unknown-state reconciliation, Pause/Stop, liquidation and
   sizing controls; inspection is not permission to connect or trade.
3. Causal splits, leakage, holdout consumption, selection accounting, and
   multi-regime after-cost acceptance.
4. Public transport persistence/resource limits, artifact lineage and current
   versus historical status routing.
5. CLI/native parity, configuration/build surfaces and remaining source/tests.

Use affected-domain verification once per behavioral checkpoint. Do not rerun
the million-line inventory or broad CI after each prose/evidence edit. Do not
claim a file was semantically reviewed merely because it parses or has tests.
The inventory's empty trailing TSV fields are intentional missing values;
its path-specific Git whitespace attribute preserves those fields. Capture
and summarize bulk diagnostic output before printing it: the first staged
whitespace check unnecessarily emitted every TSV row. Check native-command
exit codes immediately instead of allowing a later successful command to mask
a failed check. No research evidence was changed to repair that formatting check.
